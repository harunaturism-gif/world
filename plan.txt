1. **Connect Chat to WebSocket Backend**
   - Use `replace_with_git_merge_diff` to update `src/components/world/CentralPlazaEngine.tsx` to handle incoming chat messages from the WebSocket.
   - Update `Room.tsx` and `Chat.tsx` to pass messages back and forth so the UI renders remote chats.

2. **Connect Presence to WebSocket Backend**
   - Update `Room.tsx` to display the actual count of connected players from the WebSocket state instead of hardcoded numbers.

3. **Verify Frontend Compilation**
   - Use `run_in_bash_session` to run `npm run typecheck`, `npm run lint`, and `npm run build` to confirm the integration is syntactically correct.

4. **Complete pre-commit steps**
   - Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.

5. **Submit the changes**
   - Use the `submit` tool to finalize the task.